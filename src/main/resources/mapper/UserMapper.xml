<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shiro.mapper.UserMapper">

    <resultMap type="com.example.shiro.entity.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="nickname" column="nickName" jdbcType="VARCHAR"/>
        <result property="email" column="email" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 通过id查询用户数据 -->
    <select id="queryById" resultMap="UserMap">
        select id,
               nickName,
               email,
               password
        from db_doit.user
        where id = #{id}
    </select>

    <!-- 通过用户名查询用户数据 -->
    <select id="queryByName" resultMap="UserMap">
        select id,
               nickName,
               email,
               password,
               salt
        from db_doit.user
        where nickName = #{name}
    </select>

    <!--用户注册-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into db_doit.user(nickName, email, password,salt)
        values (#{nickname}, #{email}, #{password},#{salt})
    </insert>

</mapper>

